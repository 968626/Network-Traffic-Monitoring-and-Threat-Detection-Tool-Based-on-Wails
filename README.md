
**一款基于 Wails 的高性能网络流量监控与威胁检测工具**
# 源码获取：https://mbd.pub/o/bread/YZWam5xtaQ==

---

## 📖 项目简介

基于 **Wails** 框架开发的跨平台网络流量监控与威胁检测工具，集成了数据包分析、进程关联、会话流统计、威胁情报检测、地理位置可视化等功能于一体。

### 核心特性

- 🚀 **高性能抓包引擎** - 基于 gopacket/pcap 实现，每秒可捕获数千个数据包
- 🎯 **精准进程关联** - 自动将网络流量与进程绑定，支持主流操作系统
- 🛡️ **智能威胁检测** - 支持自定义威胁情报 IOC 规则，实时告警
- 🌍 **3D地理可视化** - 基于 ECharts GL 的 3D 地球和 2D 地图流量展示
- 📊 **实时仪表盘** - 大屏展示网络流量、协议分布、TOP 排行
- 💾 **数据持久化** - SQLite 存储 + PCAP 文件归档，支持历史回溯
- 🎨 **现代化界面** - Vue 3 + Element Plus + 浅色/深色主题

### 应用场景

| 场景 | 描述 |
|------|------|
| 🔍 **网络监控** | 实时监控内网流量，发现异常外联和数据泄露 |
| 🛡️ **安全威胁分析** | 检测 C2 通信、钓鱼攻击、恶意软件行为 |
| 📊 **流量审计** | 记录所有网络活动，支持取证和合规审计 |
| ⚡ **性能诊断** | 分析网络瓶颈、异常流量、协议分布 |
| 🛠️ **开发调试** | 抓包分析 HTTP/DNS/ICMP 等协议细节 |
| 🔬 **安全研究** | 恶意样本行为分析、IOC 提取 |

---

## 🎬 功能演示

### 仪表盘
![Dashboard](https://github.com/user-attachments/assets/6bd2f3ef-cd7b-40df-a03c-c9d4cbc62652)

### 会话流监控
![Session Flow](https://github.com/user-attachments/assets/2d475dbe-60b7-4e3c-acec-b220b0e28691)

### 数据包捕获
![Packet Capture](https://github.com/user-attachments/assets/e0e8fece-8818-40cf-9a73-c680d244fd3f)

### 进程关联
![Process Mapping](https://github.com/user-attachments/assets/060488a4-f12a-4a78-933d-eafd9992ff5f)



---

## 🚀 快速开始

### 环境要求

- **Go**: 1.22.0 或更高版本
- **Node.js**: 18.0.0 或更高版本
- **Npcap** (Windows) / **libpcap** (Linux/macOS)
- **Wails CLI**: 2.11.0 或更高版本

### 安装依赖

#### Windows

```powershell
# 使用 winget 安装
winget install GoLang.Go
winget install OpenJS.NodeJS
winget install Npcap.Npcap

# 安装 Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest

# 验证安装
go version
node --version
wails version
```

#### Linux (Ubuntu/Debian)

```bash
# 安装 Go
sudo apt update
sudo apt install golang-go

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 安装 libpcap
sudo apt install libpcap-dev

# 安装 Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest
```

#### macOS

```bash
# 使用 Homebrew 安装
brew install go
brew install node
brew install libpcap

# 安装 Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest
```

### 运行项目

```bash
# 克隆仓库
git clone https://github.com/vam876/FastMonitor.git
cd FastMonitor

# 安装前端依赖
cd frontend
npm install
cd ..

# 开发模式运行
wails dev

# 构建生产版本
wails build
```

⚠️ **重要**: 必须以管理员/root 权限运行，否则无法抓包！

---

## 📚 安装指南

### 从 Release 下载

1. 访问 [Releases 页面](https://github.com/vam876/FastMonitor/releases)
2. 下载对应平台的安装包
3. 安装并运行

### 从源码构建

```bash
# 克隆仓库
git clone https://github.com/vam876/FastMonitor.git
cd FastMonitor

# 安装依赖
cd frontend && npm install && cd ..

# 构建
wails build

# 运行
./sniffer  # Linux/macOS
.\sniffer.exe  # Windows
```

详细安装教程请参考：
- [Windows 安装教程](./Windows安装教程.md)
- [运行教程](./运行教程.md)

---

## 🎯 功能特性

### 1. 网卡选择

- 自动检测系统所有可用网络接口
- 支持物理网卡、虚拟网卡、回环接口、VPN 隧道
- 实时显示网卡状态和流量统计
- 一键切换抓包网卡

### 2. 实时仪表盘

- 实时流量曲线图（上下行 bps/pps）
- 协议分布饼图（TCP/UDP/ICMP/DNS/HTTP）
- TOP 源地址 / 目标地址 / 进程排行
- 地理分布热力图
- 告警统计（Critical/Warning/Info）
- 大屏模式支持

### 3. 数据包捕获

- 每秒捕获数千个数据包并解码
- 多层协议解析（Ethernet → IP → TCP/UDP → HTTP/DNS）
- BPF 过滤器支持
- 数据包详情查看（十六进制/ASCII）
- 导出为 PCAP 格式

### 4. DNS 解析

- 解析 A/AAAA/CNAME/MX/TXT 等记录类型
- 查询统计（最频繁查询的域名 TOP10）
- 响应时间记录
- 失败查询记录（NXDOMAIN/SERVFAIL）
- 恶意域名检测

### 5. HTTP 流量

- HTTP 请求/响应解析
- 提取 Method/URL/Headers/Body
- HTTPS 元数据提取（SNI 域名）
- 恶意 URL 检测
- User-Agent 分析

### 6. ICMP 流量

- ICMP 类型识别（Echo Request/Reply、Destination Unreachable）
- Ping 统计（往返时延 RTT、丢包率）
- 网络诊断（路由问题、MTU 问题）
- 异常检测（ICMP Flood 攻击）

### 7. 会话流统计

- 五元组聚合（SrcIP, DstIP, SrcPort, DstPort, Protocol）
- 双向流量统计
- 会话时长记录
- 地理位置查询
- 进程绑定
- TCP 连接状态追踪

### 8. 进程关联

- 进程枚举（遍历系统所有进程及其网络连接）
- 连接映射（将 IP, Port 映射到进程 PID）
- 进程信息提取（进程名、路径、命令行、启动时间）
- 进程统计（每个进程的网络流量和连接数）
- 进程告警（检测恶意进程的网络行为）

### 9. 威胁检测

- 自定义威胁情报 IOC 规则
- 实时告警（Critical/Warning/Info）
- 内置威胁规则（银狐病毒 C2 域名、恶意 PNG 下载等）
- 规则管理（添加、编辑、删除、启用/禁用）

---

## 🏗️ 技术栈

### 前端

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.4.0 | 渐进式 JavaScript 框架 |
| TypeScript | 5.3.0 | JavaScript 的超集，提供类型安全 |
| Element Plus | 2.5.0 | Vue 3 组件库 |
| Pinia | 2.1.7 | Vue 3 官方状态管理库 |
| Vue Router | 4.2.5 | Vue 3 官方路由管理器 |
| ECharts | 5.6.0 | 数据可视化库 |
| Vite | 5.0.0 | 下一代前端构建工具 |

### 后端

| 技术 | 版本 | 说明 |
|------|------|------|
| Go | 1.22+ | 高性能编程语言 |
| Wails | 2.11.0 | 跨平台桌面应用框架 |
| gopacket | 1.1.19 | Go 语言的网络数据包处理库 |
| miekg/dns | 1.1.62 | Go 语言的 DNS 库 |
| gopsutil | 3.24.5 | 跨平台的进程和系统监控库 |
| SQLite | 1.34.4 | 轻量级嵌入式数据库 |

### 系统依赖

| 平台 | 依赖 |
|------|------|
| Windows | Npcap |
| Linux | libpcap-dev |
| macOS | libpcap |

---

## 📁 项目结构

```
FastMonitor/
├── cmd/
│   └── sniffer/              # 主程序入口
│       └── main.go
├── frontend/                 # 前端源码
│   ├── public/              # 静态资源
│   ├── src/
│   │   ├── components/       # Vue 组件
│   │   ├── views/           # 页面视图
│   │   ├── router/          # 路由配置
│   │   ├── stores/          # 状态管理
│   │   ├── i18n/           # 国际化
│   │   └── wailsjs/        # Wails 生成的绑定代码
│   ├── package.json         # 前端依赖
│   └── vite.config.ts      # Vite 配置
├── internal/                # 后端内部包
│   ├── capture/             # 抓包引擎
│   ├── parser/              # 协议解析
│   ├── process/             # 进程映射
│   ├── server/              # RPC 服务
│   ├── store/               # 数据存储
│   ├── cache/               # 缓存
│   ├── netio/               # 网络 I/O
│   ├── config/              # 配置管理
│   └── scheduler/           # 调度器
├── pkg/                     # 公共包
│   ├── model/               # 数据模型
│   └── bytesize/            # 字节大小转换
├── build/                   # 构建配置
│   ├── windows/             # Windows 构建配置
│   ├── darwin/              # macOS 构建配置
│   └── linux/               # Linux 构建配置
├── config.yaml              # 配置文件
├── go.mod                   # Go 依赖
├── go.sum                   # Go 依赖锁定
├── wails.json               # Wails 配置
└── main.go                  # Wails 入口
```

---

## ⚙️ 配置说明

### config.yaml

```yaml
# 数据库配置
database:
  path: "data/sniffer.db"

# 抓包配置
capture:
  device: ""                # 网卡设备名称（空表示自动选择）
  snaplen: 65535           # 抓包长度
  promiscuous: true         # 混杂模式
  timeout: 30s             # 超时时间

# 存储配置
storage:
  retention_days: 7         # 数据保留天数
  max_packets: 100000      # 最大数据包数量
  pcap_dir: "data/pcap"    # PCAP 文件目录

# 调度器配置
scheduler:
  vacuum_interval: 1h       # 清理间隔

# 缓冲区限制
limits:
  raw_max: 10000           # 原始数据包最大数量
  dns_max: 1000            # DNS 记录最大数量
  http_max: 1000           # HTTP 记录最大数量
  icmp_max: 1000           # ICMP 记录最大数量
```


